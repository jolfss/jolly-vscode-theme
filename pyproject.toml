[project]
name = "jolly-theme"
version = "0.1.0"
description = "VSCode theme builder for Jolly Light theme"
requires-python = ">=3.8"
dependencies = [
    "json5>=0.9.0",
]

[tool.uv]
package = false
