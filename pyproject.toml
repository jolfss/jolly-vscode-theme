[project]
name = "jolly-theme"
version = "0.1.0"
description = "VS Code theme builder for Jolly light and dark themes"
requires-python = ">=3.12"
dependencies = [
    "json5>=0.9.0",
]

[tool.uv]
package = false
